// when a user clicks a workout in the Grid,
// it pops up a screen, it allows them to see the exercise
// input the weights; and allows them to save the exercise
//all of this logic is contained in this reusable workout card

export default function WorkoutCard() {
  return <></>;
}
